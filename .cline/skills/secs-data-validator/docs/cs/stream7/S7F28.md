# S7F28 - Process Program Verification Acknowledge

**Source:** [SECS-II Automated Code Generation Tool](http://hume.com/secs/msgsCS.html)

**Sent by:** Host Only

## Format

_header only_

## Details

Reply to S7F27R. Acknowledges the process program verification. No data items in the reply.

## C# Code

```csharp
    // S7F28 C# Parse reply
    SecsPort sp=(SecsPort)your_SecsPort
    RcResult rcr = sp.SendSecsMsg(7, 27, true, sendData, true);
    string TSN_data;
    bool ok=true;
    while (ok) {   // break out of loop on error
        if (rcr.rc != 0) {ok=false; break;}
        TSN_data = rcr.result;
        // no data expected
        return;  // parsed ok
        } // end while(ok)
```
